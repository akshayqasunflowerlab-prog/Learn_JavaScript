/*
What is function?
A function in JavaScript is a reusable block of code that performs a specific task. Instead of writing the same code multiple times, you can write it once inside a function and call (invoke) it whenever needed.

Functions make programs:

Reusable – Write once, use many times.
Modular – Divide large programs into smaller parts.
Easy to Read – Code becomes more organized.
Easy to Maintain – Changes are made in one place instead of multiple places.
Syntax of a Function
function functionName(parameters) {
    // code to execute
    return value;   // optional
}
Explanation
function → Keyword used to declare a function.
functionName → Name of the function.
parameters → Inputs received by the function (optional).
return → Sends a value back to the caller (optional).
*/